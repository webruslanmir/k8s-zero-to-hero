project: k8s-zero-to-hero
configVersion: 1
---
image: ubuntu-test
from: ubuntu:22.04
build:
  platform: 
  - 'linux/amd64'
git:
- add: /
  to: /
shell:
  install:
  - apt update
  - apt install -y curl vim
  setup:
  - echo "Hello, world"
