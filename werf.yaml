project: k8s-zero-to-hero
configVersion: 1
build:
  platform: 
  - 'linux/amd64'
---
image: ubuntu-test
from: ubuntu:22.04
git:
- add: /
  to: /
shell:
  install:
    install:
  - apt update && apt install -y curl vim python3 python3-pip && rm -rf /var/lib/apt/lists/*
  setup:
  - python3 -c "print('Hello, world')"
